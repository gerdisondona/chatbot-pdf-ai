from langchain.chains import RetrievalQA
from langchain.chat_models import ChatOpenAI

# Criar modelo de chat
modelo_chat = ChatOpenAI(temperature=0)

# Criar chain de perguntas e respostas usando vetor de busca
qa = RetrievalQA.from_chain_type(
    llm=modelo_chat,
    retriever=vetor_index.as_retriever()
)

# Testar pergunta
resposta = qa.run("Qual é o tema principal do PDF?")
print(resposta)
