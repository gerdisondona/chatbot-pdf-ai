from PyPDF2 import PdfReader

def ler_pdf(caminho_arquivo):
    leitor = PdfReader(caminho_arquivo)
    texto = ""
    for pagina in leitor.pages:
        texto += pagina.extract_text()
    return texto

# Exemplo
texto_pdf = ler_pdf("inputs/exemplo.pdf")
print(texto_pdf)
